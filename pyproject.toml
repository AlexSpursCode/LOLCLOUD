[project]
name = "lolcloud"
version = "0.1.0"
description = "Cloud Living Off the Land catalog and validation framework"
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
